import styled from 'styled-components';

const SearchPagination = () => {
  return <StyledPaginatonWapper></StyledPaginatonWapper>;
};

export default SearchPagination;

const StyledPaginatonWapper = styled.div`
  display: flex;
`;
