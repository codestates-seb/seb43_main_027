import styled from 'styled-components';
import { LabelType } from '../../types/componentsTypes';

export const Label = styled.label<LabelType>`
  font-size: 1.6rem;
  margin: 1rem 1rem;
  padding: 0.5rem 0.5rem;
  border-radius: 4px;
`;

export default Label;
